spring:
  application:
    name: notification-service

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  cloud:
    function:
      definition: notifyUserAsOrderIsCreated
    stream:
      bindings:
        notifyUserAsOrderIsCreated-in-0:
        #          destination: order.created
        notifyUserAsOrderIsCreated-out-0:
      #          destination: order.ack
      default:
        content-type: application/json
        group: ${spring.application.name}

  mail:
    host: smtp.gmail.com
    port: 587
    username: habittrackerwebsite.app@gmail.com
    password: iwmkqhkgdskuubbs
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

server:
  port: 8083



